# 销售预测滚动选模与独立测试

系统使用 3 个扩展窗口做滚动验证，按平均 MAPE
选择 `Prophet`，验证 MAPE 为 9.12%
（标准差 1.29）。

最终 6 个月完全不参与选模。`Prophet` 在该独立测试集上的 MAPE 为
15.65%；测试集事后最优模型是 `LastValue`，MAPE 为
14.36%。该差异用于如实展示
模型选择的不确定性，不能反向用测试集重选模型。

完成独立测试后，运行时会把验证阶段选定的模型在全部已观测历史上重新拟合，再生成
真正的未来预测。
