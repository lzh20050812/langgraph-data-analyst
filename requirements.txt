# ============================================================
# 核心依赖
# ============================================================

# LangGraph / LangChain（多智能体编排）
langgraph==1.2.10
langchain==1.3.14
langchain-core==1.5.3

# LLM（DeepSeek-V3，兼容 OpenAI API）
openai==2.53.0

# 数据库
sqlalchemy==2.0.51
pymysql==1.2.0
chromadb==1.5.9

# Embedding（BAAI/bge-small-zh，本地运行）
sentence-transformers==5.7.0

# 数据处理
pandas==3.0.5
numpy==2.4.6; python_version < "3.12"
numpy==2.5.2; python_version >= "3.12"

# 机器学习与预测
scikit-learn==1.9.0
# Use the official minimal CPU distribution in the amd64 Linux container.
# The full Linux wheel now pulls NCCL even though this project does not use GPU training.
xgboost-cpu==3.2.0; platform_system == "Linux" and platform_machine == "x86_64"
xgboost==3.4.0; platform_system != "Linux" or platform_machine != "x86_64"
prophet==1.3.0

# Web 框架
fastapi==0.141.1
uvicorn==0.52.1

# 环境变量
python-dotenv==1.2.2

# SQL 解析（安全校验）
sqlparse==0.5.5
sqlglot==30.17.0

# 日期处理
python-dateutil==2.9.0.post0
